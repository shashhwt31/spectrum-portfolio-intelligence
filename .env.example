# Reserved for a future server-side integration layer.
# Never put browser-visible broker credentials, passwords, OTPs, PINs, TPINs,
# or trade authorization secrets in this file.

# MARKET_DATA_PROVIDER=licensed-provider-name
# MARKET_DATA_API_BASE_URL=https://api.example.com
# APP_ENV=development
